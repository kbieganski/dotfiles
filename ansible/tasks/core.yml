- name: Install packages
  pacman:
    name:
      - base-devel
      - cron
      - git
      - openssh
  become: true
- name: Create symlink for git config
  file:
    src: "{{DOTFILES_DIR}}/git.conf"
    dest: ~/.gitconfig
    state: link
